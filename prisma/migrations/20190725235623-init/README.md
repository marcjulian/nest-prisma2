# Migration `20190725235623-init`

This migration has been generated by Marc Stammerjohann at 7/25/2019, 11:56:23 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "dev"."User"("id" TEXT NOT NULL  ,"email" TEXT NOT NULL DEFAULT '' ,"password" TEXT NOT NULL DEFAULT '' ,"firstname" TEXT NOT NULL DEFAULT '' ,"lastname" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "dev"."User.id._UNIQUE" ON "User"("id")

CREATE UNIQUE INDEX "dev"."User.email._UNIQUE" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190725235623-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,16 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:../db/dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id        String @default(cuid()) @id @unique
+  email     String @unique
+  password  String
+  firstname String
+  lastname  String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190725235623-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190725235623-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
